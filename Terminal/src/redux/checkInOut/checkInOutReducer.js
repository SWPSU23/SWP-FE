import {ACT_CHECK_IN} from './action';

const initialState = {
	// isCashier: true,
};

const checkInOutReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default checkInOutReducer;
